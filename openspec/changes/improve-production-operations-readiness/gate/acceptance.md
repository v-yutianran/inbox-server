# 生产运维就绪改进验收设计

## 文档信息

| 项目 | 内容 |
| --- | --- |
| 验收对象 | OpenSpec change `improve-production-operations-readiness` |
| 版本/提交 | 文档基线为分支 `docs/production-operations-readiness-plan`；实现验收开始时必须冻结并记录实际提交、Cloudflare version、镜像 digest 与 Sealos revision |
| 环境 | 本地自动化、隔离集成环境、隔离恢复环境；生产环境默认只读 |
| 日期 | 2026-08-03 |
| 状态 | 验收设计已完成；实现与验收尚未执行 |
| 责任人 | 实现：开发人员；执行：独立验收人员；线上动作：获授权运维/发布人员 |
| 需求基线 | `requirements.md` 1.0 |
| 设计基线 | `technical-design.md` 1.0 |
| 关联资料 | `../specs/production-operations-readiness/spec.md`、`../tasks.md`、`../proposal.md`、`../design.md` |

## 验收范围与排除项

范围包括分层健康、探针隔离、任务领取门禁、队列/DLQ、逐条重放、隔离 canary、SLI/SLO/告警、稳定事件、发布回滚、保留治理、配置权限、容量成本、备份恢复及旧资产决策。

排除业务语义变更、多副本实际启用、外部 APM/Cloudflare Access 引入，以及任何未授权的生产部署、重放、清理、回滚、删除或真实数据试验。

自动验收只允许合成数据、隔离存储和无副作用 destination；真实用户数据、真实凭据、真实 URL/标题/正文及真实外部目标写入均禁止。

## 基线快照与可漂移性

- 需求记录的 2026-08-03T00:46:21Z 附近只读基线为：job `dead=224`、`deferred=93`、`done=4912`、`failed=1`、`processing=1`，DLQ `238`，replay operation `0`，D1 `8,540,160` 字节，Worker 心跳更新中。
- 上述数值只用于解释当前风险，不是验收阈值，也不得直接作为执行证据。
- 每次验收须重新冻结 `freezeAt`、部署版本、各状态计数、稳定摘要及查询条件；线上变化不等同于缺陷。
- 只读报告须记录查询前后计数/稳定摘要；发生漂移时废弃旧计划并重新冻结，不得静默沿用旧 `planHash`。

## 准入条件

1. 需求、技术设计、spec 与 tasks 已读回一致，实际提交和部署版本已冻结。
2. 实现完成适用的单元、集成、契约、组件测试，结果可复现且测试数据全为合成数据。
3. 隔离环境、替身依赖、无副作用 sink、可恢复备份及证据目录可用。
4. 所有证据先执行敏感哨兵扫描，凭据、Cookie、storage state、payload 与用户内容命中数为 0。
5. 涉及线上或浏览器 E2E 的用例已取得对应独立人工授权；未授权用例不得执行。
6. 24 小时与 14 天观察窗从明确时间点开始连续采样；历史零散记录不得拼接冒充完整窗口。

## 退出与判定条件

- `通过`：全部 P0、适用 P1/P2 用例实际执行通过，强制观察窗完整，敏感命中和真实副作用均为 0，无未解释差异。
- `有条件通过`：P0 全通过，仅非发布阻断的 P2 决策项待后续批准，且有责任人、期限和已接受残余风险；不得用于掩盖未满观察窗。
- `不通过`：任一 P0 失败，出现真实副作用、敏感泄漏、未解释差异、版本/数据被失败门禁改变，或 RTO 超过 15 分钟。
- `阻塞`：环境/证据/授权缺失，24 小时或 14 天窗口未满，告警渠道、保留期或 RPO 尚未批准且相应用例无法判定。
- 单元、集成、契约、组件、E2E 与运行时证据分别记录；低层通过不得替代高层和观察窗证据。

## 独立人工授权门禁

| 门禁 ID | 动作 | 当前状态 | 放行要求 |
| --- | --- | --- | --- |
| AUTH-001 | 浏览器自动化 E2E/线上 canary | 未授权 | 当前任务明确授权自动化 E2E，限定环境、fixture 与无副作用 sink |
| AUTH-002 | 生产真实重放 | 未授权 | 精确对象、同参数 dry-run、`planHash`、幂等/限速/权限核对及显式执行确认 |
| AUTH-003 | 生产真实清理 | 未授权 | 完整 14 天 dry-run、期限批准、备份、分批计划、前后对账及显式删除确认 |
| AUTH-004 | 生产发布 | 未授权 | 固定 release manifest、全部预检通过、变更范围与显式发布确认 |
| AUTH-005 | 生产回滚 | 未授权 | 精确版本、兼容性/备份证据、影响范围与显式回滚确认 |
| AUTH-006 | 旧运行时、历史数据或部署资产删除 | 未授权 | 回滚窗口结束、备份可恢复、精确目标清单与单独删除授权 |
| AUTH-007 | 真实数据复制、重放或试验性恢复 | 未授权 | 明确数据范围与隔离方案；默认仍禁止使用真实用户数据 |

各门禁互不继承；一次授权不覆盖其它动作或后续任务。未授权仅使对应执行用例阻塞，不阻塞实现。

## 需求—设计—验收追踪矩阵

| 需求 | AC | 技术设计章节 | 用例 | 计划证据 | 当前结果 |
| --- | --- | --- | --- | --- | --- |
| REQ-P0-001 | AC-001 | 健康状态机、运维接口 | AT-P0-001 | `health/components.json`、`probe-24h.csv` | 未执行 |
| REQ-P0-002 | AC-001 | 健康状态机、验证策略 | AT-P0-001 | `probe-load-report.md` | 未执行 |
| REQ-P0-003 | AC-002 | 健康状态机、组件职责 | AT-P0-002 | `dependency-gating.json` | 未执行 |
| REQ-P0-004 | AC-003 | 运维接口、SLI 数据模型 | AT-P0-003 | `queue-summary.json` | 未执行 |
| REQ-P0-005 | AC-004 | DLQ 一致性 | AT-P0-004 | `dlq-consistency.json` | 未执行 |
| REQ-P0-006 | AC-005 | 安全重放 | AT-P0-005 | `replay-plan.json` | 未执行 |
| REQ-P0-007 | AC-005 | 重放状态机、queue inbox | AT-P0-005 | `replay-audit.json` | 未执行 |
| REQ-P0-008 | AC-006 | 隔离 canary | AT-P0-006 | `canary-report.json` | 未执行 |
| REQ-P0-009 | AC-007 | 发布回滚 CLI、兼容回滚 | AT-P0-007 | `isolated-rollback.md` | 未执行 |
| REQ-P1-001 | AC-003/008 | 指标、SLI/SLO | AT-P0-003、AT-P1-001 | `metrics.json`、`baseline-14d.csv` | 未执行 |
| REQ-P1-002 | AC-008 | SLO policy | AT-P1-001 | `slo-approval.md` | 未执行 |
| REQ-P1-003 | AC-008 | 告警状态机 | AT-P1-001 | `alert-recovery.json` | 未执行 |
| REQ-P1-004 | AC-009 | 可观测性与安全 | AT-P1-002 | `event-trace.json` | 未执行 |
| REQ-P1-005 | AC-010 | 发布与回滚 CLI | AT-P1-003 | `release-plan.json` | 未执行 |
| REQ-P1-006 | AC-010 | 发布阶段与停止条件 | AT-P1-003 | `failed-gate-diff.json` | 未执行 |
| REQ-P1-007 | AC-007 | 发布回滚 CLI | AT-P0-007 | `rto-timeline.json` | 未执行 |
| REQ-P1-008 | AC-011 | 数据保留、备份恢复 | AT-P1-004 | `retention-14d.csv` | 未执行 |
| REQ-P1-009 | AC-012 | 配置与安全边界 | AT-P1-005 | `startup-gates.json` | 未执行 |
| REQ-P1-010 | AC-005/012 | 运维接口、权限边界 | AT-P0-005、AT-P1-005 | `rbac-audit.json` | 未执行 |
| REQ-P2-001 | AC-013 | 容量/成本指标 | AT-P2-001 | `capacity-report.json` | 未执行 |
| REQ-P2-002 | AC-013 | 单副本决定 | AT-P2-001 | `capacity-alert.json` | 未执行 |
| REQ-P2-003 | AC-014 | 关键决定与权衡 | AT-P2-002 | `replica-admission.md` | 未执行 |
| REQ-P2-004 | AC-015 | 数据保留、备份恢复 | AT-P2-003 | `restore-reconcile.json` | 未执行 |
| REQ-P2-005 | AC-016 | 旧资产保留 | AT-P2-004 | `legacy-assets.md` | 未执行 |
| NFR-001 | AC-001 | 健康状态机 | AT-P0-001 | `probe-24h.csv` | 未执行 |
| NFR-002 | AC-007/015 | 回滚、备份恢复 | AT-P0-007、AT-P2-003 | `rto-timeline.json` | 未执行 |
| NFR-003/004 | AC-009/012 | 可观测性与安全 | AT-P1-002、AT-P1-005 | `sensitive-scan.json` | 未执行 |
| NFR-005 | AC-006 | 隔离 canary | AT-P0-006 | `side-effect-audit.json` | 未执行 |
| NFR-006 | AC-017 | 迁移与兼容回滚 | AT-P1-006 | `contract-regression.json` | 未执行 |
| NFR-007 | AC-013 | 容量/成本指标 | AT-P2-001 | `cost-trend.json` | 未执行 |

所有证据统一放入 `evidence/acceptance/<run-id>/`；实际路径、哈希、生成命令、时间和执行人写入执行记录，未生成文件不得视为证据。

## P0 验收用例

### AT-P0-001 分层健康与长任务探针
- 前置/数据：隔离 Worker、六组件替身、固定长耗时浏览器 fixture；启动完整连续 24 小时观察窗。
- 步骤：启动长任务并按生产预算探测 liveness/readiness；切换五态并验证过期快照。
- 预期：探针超时数为 0；六组件状态、原因、时间、版本、`canAcceptWork` 可判定；过期状态不为 ready。
- 证据：探针原始序列、状态响应、部署版本、窗口起止及敏感扫描；窗口未满只能标记未验证。
- 自动化层级：单元+契约+组件+24 小时运行时证据；浏览器 E2E 受 AUTH-001 限制。

### AT-P0-002 依赖失效领取隔离
- 前置/数据：依赖/不依赖 browser、Mihomo、WARP 的合成任务各一组，无副作用 sink。
- 步骤：分别置依赖为未就绪，触发领取并核对已领取任务的完成、延期或租约释放。
- 预期：依赖任务不被新领取或被安全延期；无关任务正常；重复副作用与真实外部调用均为 0。
- 证据：领取决策、租约变化、稳定事件、sink 调用计数。
- 自动化层级：单元+集成+组件；不需要生产写入。

### AT-P0-003 队列分类与趋势一致性
- 前置/数据：含可执行、处理中、延迟、不可执行任务的冻结合成集及只读线上快照。
- 步骤：查询 summary/metrics，对照源数据计算数量、最老年龄、最早恢复时间和趋势。
- 预期：分类互斥且总数可对账；当前值、趋势、阈值状态、采集时间和部署版本齐全。
- 证据：查询响应、独立计算结果、freezeAt 与只读前后摘要。
- 自动化层级：集成+契约；生产仅只读。

### AT-P0-004 DLQ 全量一致性
- 前置/数据：按 freezeAt 冻结 DLQ、dead job、恢复信封和 replay 最小元数据。
- 步骤：生成报告，逐条验证七类之一且互斥，复核分类和总计。
- 预期：`unexplainedCount=0`，总量对账；异常可分类但不得伪装已解决；记录零修改。
- 证据：一致性报告、稳定 ID 摘要、查询前后摘要和只读审计。
- 自动化层级：集成+契约+生产只读核对。

### AT-P0-005 重放鉴权、dry-run 与幂等
- 前置/数据：隔离的可重放/材料缺失/幂等冲突/限速合成记录；生产执行需 AUTH-002。
- 步骤：依次无认证、只读身份、plan、篡改参数 execute、正确 execute、重复 operationKey。
- 预期：越权拒绝；plan 零写入；旧哈希 stale；正确计划只发布一次；状态和审计主体完整。
- 证据：响应、planHash、queue inbox 计数、审计状态、sink 计数及敏感扫描。
- 自动化层级：单元+集成+契约；生产真实重放未授权即阻塞，不得替代执行。

### AT-P0-006 隔离 canary 零副作用
- 前置/数据：固定合成 fixture、隔离 D1/Queue、dry-run destination，AUTH-001 已放行方可跑浏览器 E2E。
- 步骤：运行入队、领取、direct 成功、短内容拒绝、浏览器回退、归档模拟和结算。
- 预期：各分支可关联并正确结算；真实 Cubox、Flomo、坚果云、文章仓库写入均为 0。
- 证据：canary 事件链、隔离存储对账、网络/sink 调用审计、敏感扫描。
- 自动化层级：集成+隔离 E2E；当前 AUTH-001 未授权，E2E 结果为未执行。

### AT-P0-007 隔离回滚与 RTO
- 前置/数据：可恢复隔离备份、上一 Cloudflare version/Worker digest、双版本兼容合成数据。
- 步骤：制造退出门槛失败，执行隔离回滚并验证健康、积压、数据兼容和关键流程。
- 预期：15 分钟内恢复，记录数/稳定摘要一致，生产数据与旧资产零变化。
- 证据：时间线、manifest/planHash、版本读回、兼容查询和对账摘要。
- 自动化层级：组件+隔离 E2E；生产回滚另受 AUTH-005 限制。

## P1 验收用例

### AT-P1-001 14 天 SLI/SLO 与告警恢复
- 前置/数据：完整连续 14 天脱敏样本、已批准渠道和候选/批准 policy；零副作用越界注入器。
- 步骤：核对 SLI，批准阈值后制造越界并恢复，检查 pending/firing/recovered 去重。
- 预期：当前值、趋势、阈值、采集时间、版本齐全；告警/恢复各一次且敏感命中为 0。
- 证据：14 天原始聚合、批准记录、通知回执和敏感扫描；窗口未满不得通过。
- 自动化层级：单元+集成+隔离 E2E+14 天运行时证据。

### AT-P1-002 稳定事件追踪与敏感哨兵
- 前置/数据：覆盖领取、续租、延期、重试、死信、完成的合成任务及敏感哨兵串。
- 步骤：汇总 API/Worker 事件，按关联 ID 重建状态链并扫描全部产物。
- 预期：事件名符合点分规范，关联链无断点；日志、指标、告警、Console、证据敏感命中为 0。
- 证据：事件目录、重建报告、扫描范围与零命中报告。
- 自动化层级：单元+集成+契约。

### AT-P1-003 发布计划一致、失败停止
- 前置/数据：固定 release manifest、有效/无效 migration、digest、Secret 引用、备份和 canary 结果。
- 步骤：同输入 plan/dry-run；模拟各门禁失败；在隔离目标执行成功计划并读回版本。
- 预期：planHash 一致；任一预检失败时版本/数据零变化；成功证据完整脱敏且不跳步。
- 证据：计划 diff、步骤状态、前后版本/数据摘要、发布清单和扫描结果。
- 自动化层级：单元+脚本集成+隔离 E2E；生产发布/回滚受 AUTH-004/005 限制。

### AT-P1-004 保留报告与分批清理
- 前置/数据：连续 14 天同参数 dry-run、已批准期限、隔离数据、可恢复备份；生产清理需 AUTH-003。
- 步骤：核对候选，隔离执行分批清理，中断后恢复，再次执行验证幂等并对账。
- 预期：仅批准状态/期限记录被处理；可中断、无长事务；幂等、审计、重试窗口不受破坏。
- 证据：14 天报告、批准记录、游标、批次审计、前后计数/摘要和恢复证据。
- 自动化层级：单元+集成+隔离恢复；生产清理当前未授权。

### AT-P1-005 配置快速失败与权限隔离
- 前置/数据：缺失/误配 Secret、管理/Worker/只读身份矩阵和敏感哨兵。
- 步骤：逐项启动或调用只读、重放、清理、发布、回滚入口，检查浏览器会话退出后状态。
- 预期：误配快速失败且只显示脱敏项名；越权请求拒绝；写操作显式确认并记录审计主体。
- 证据：启动结果、HTTP 响应、权限矩阵、审计和构建/存储敏感扫描。
- 自动化层级：单元+契约+组件。

### AT-P1-006 公开契约与业务回归
- 前置/数据：既有 API、采集、分发、限速、归档回归集和固定合成期望。
- 步骤：在基线与候选版本运行同一回归并比较路径、状态码、业务结果和副作用摘要。
- 预期：公开契约与业务语义无非预期变化，数据可跨版本读取，真实外部调用为 0。
- 证据：基线/候选报告、契约 diff、兼容读取与副作用审计。
- 自动化层级：单元+集成+契约；浏览器场景受 AUTH-001 限制。

## P2 验收用例

### AT-P2-001 单副本容量与成本
- 前置/数据：无真实目标负载模型，Worker 副本固定为 1，已批准或候选容量阈值。
- 步骤：逐步提高到达率并注入长浏览器任务/代理重连，采集资源、时延、吞吐和成本趋势。
- 预期：P50/P95、到达/消化率、积压、D1/日志用量可观察并告警；不自动扩容。
- 证据：负载参数、资源时序、容量告警、成本估算和副本读回。
- 自动化层级：隔离性能测试+运行时证据；不得使用真实目标。

### AT-P2-002 多副本准入阻断
- 前置/数据：租约竞争、重复投递、副作用幂等、归档冲突、登录态隔离、故障转移合成场景。
- 步骤：逐项运行并提交扩容申请，分别模拟任一失败和全部通过。
- 预期：任一失败均阻断；全部通过仍须独立 ADR 获批，本变更保持副本 1。
- 证据：并发测试、准入决策、replica 读回和 ADR 状态。
- 自动化层级：集成+隔离并发测试+人工 ADR 门禁。

### AT-P2-003 Worker/浏览器/WARP 备份恢复
- 前置/数据：已批准 RPO、可恢复快照、隔离环境和稳定 ID/记录数基线。
- 步骤：恢复三类状态，启动门禁并运行关键合成流程，测量 RTO/RPO 并对账。
- 预期：RTO 不超过 15 分钟，数据不超 RPO，稳定 ID/记录数一致，敏感命中与真实副作用为 0。
- 证据：快照标识、恢复时间线、版本/schema、对账与扫描结果。
- 自动化层级：隔离恢复演练；RPO 未批准时只能未验证，生产恢复需单独授权。

### AT-P2-004 旧资产零删除
- 前置/数据：旧 Python、Docker 数据和部署资产精确清单、回滚窗口与备份状态。
- 步骤：只读核对清单和删除授权，生成保留/移除建议，不执行删除。
- 预期：AUTH-006 未放行时旧资产删除数和改写数均为 0，仅形成可追踪结论。
- 证据：只读清单、备份/窗口状态、建议与变更前后摘要。
- 自动化层级：只读检查+人工授权门禁。

## 执行记录、未执行项与残余风险

- 本文是验收设计，不是验收执行报告；以上用例当前均为“未执行”，不存在“通过”事实。
- 24 小时探针窗口、14 天 SLI/SLO 基线、14 天 retention dry-run 均未在本文中宣告完成。
- 告警渠道/责任人、最终 SLO、保留期、生产 RPO 和多副本路线只阻塞对应最终判定，不阻塞实现。
- 单副本仍有可用性上限；在容量、备份恢复和多副本 ADR 完成前保持已知残余风险。
- 实施若改变需求、接口、数据契约、架构边界或验收标准，必须从最早受影响门禁重新评审。

## 覆盖统计

- 需求覆盖：REQ-P0 9/9、REQ-P1 10/10、REQ-P2 5/5、NFR 7/7 均映射设计章节、AC、用例和计划证据。
- 验收标准覆盖：AC-001～AC-017 共 17/17；用例 17 个，其中 P0 7、P1 6、P2 4。
- 已执行通过：0；未执行：17；敏感信息允许命中：0；真实外部副作用允许次数：0。

## 阻塞项

- 实现阻塞项：无。
- 当前执行阻塞：AUTH-001～AUTH-007 未授权；24 小时与14 天观察窗尚无完整执行证据；最终告警渠道、SLO、保留期和 RPO 待相应阶段批准。

## 最终结论

**验收设计门禁通过，可进入实现；验收执行结论为阻塞。** 阻塞仅来自尚未实现、未授权及观察窗未完成，不得推定任何运行时用例已通过。
