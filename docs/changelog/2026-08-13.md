# 2026-08-13 Changelog

### fix(worker)：放宽 Xvfb 冷启动就绪窗口

**改了什么**：

- Worker entrypoint 单次等待 X11 socket 的窗口从 5 秒扩大到 30 秒；仍保留最多 5 次重试、清理陈旧 lock/socket、失败后回收子进程，以及成功后由 Node Worker 接管 PID 1。
- 新增基于公开进程行为的 subprocess 测试：假的 Xvfb 在 6 秒后创建 Unix socket 时必须继续启动 Worker；永久失败时必须非零退出并输出唯一低基数事件 `xvfb_failed`。
- 不新增 Span、Metric、日志属性或配置字段，不改变文章处理、IMA 格式、代理路由、PVC、Secret 或副本数。

**如何验证**：

- RED：`uv run pytest tests/unit/test_deployment_files.py::test_typescript_worker_entrypoint_waits_for_slow_xvfb_socket -q`，现有实现因 5 秒窗口稳定失败并输出 `xvfb_failed`。
- GREEN：慢启动与既有 entrypoint 契约 2/2 通过。
- REFACTOR：`uv run pytest tests/unit/test_deployment_files.py -q`（9 passed）。
- Python ruff 全绿，unit/integration 263/263 通过（保留 9 条既有 warning），mypy 对 103 个源文件无问题。
- TypeScript API 66、Console 15、Worker 123、Domain 8、Release 30 全部通过；`npm run typecheck` 与 `npm run build` 通过。
- OpenSpec strict valid；linux/amd64 Worker 镜像构建成功，并在 UID/GID 1001、drop ALL、no-new-privileges 下验证 X11 socket 就绪。
- `git diff --check` 与冲突标记检查通过；GitNexus compare 风险 LOW、0 条执行流受影响。

**结果与残余风险**：

- 同一 Worker 镜像在本机以 UID/GID 1001、drop ALL 和 no-new-privileges 可立即创建 X11 socket；线上 Sealos 新 Pod 的 Worker 连续 5 次以 `xvfb_failed` 退出，而 mihomo、WARP 已 Ready，故修复限定为资源受限节点的 Xvfb 启动预算。
- OpenSpec 的连续 24 小时无探针超时验收仍未完成；本次不会提前勾选任务 2.8。
- `npm ci` 报告锁定依赖存在 9 个 moderate、2 个 high 审计漏洞，Worker 生产裁剪阶段报告 1 个 high；本次未修改依赖且未自动升级，保留为既有供应链风险。
- 首次并行执行 `npm test` 时 `@inbox/domain/dist` 尚未生成，API/Worker 测试仅因模块无法解析失败；按项目既有顺序完成 build 后重跑全绿，不属于功能回归。

### fix(worker)：按 IMA 真实字段识别月份文件夹

**改了什么**：

- 修正 `get_knowledge_list` 响应映射：月份名称来自 `title`，文件夹标识来自 `media_id`；后续重名预检和添加知识仍按 IMA 请求契约发送为 `folder_id`。
- 保留唯一匹配和 `folder_` 前缀校验；零个、多个或非法标识继续使用既有 `folder_not_unique` 原因码 fail closed。
- 同步 OpenSpec 设计与场景措辞，不改变远程请求数、错误阶段、低基数日志或 Git 权威归档。

**如何验证**：

- 线上只读诊断：`get_knowledge_list` 分 2 页返回 75 条，所有条目字段集合均为 `media_id,media_type,parent_folder_id,tags,title`；`title=202608` 恰好 1 条，`name` 与 `folder_id` 均不存在。
- RED：`npm run test --workspace @inbox/worker -- --run tests/ima-article-mirror.test.ts`，9 项中 4 项精确失败于 `ima_month_folder_not_unique`。
- GREEN：同命令 9/9；先构建 `@inbox/domain` 后，Worker 123/123、TypeScript 全工作区 242/242、Python unit/integration 261/261 全通过。
- `npm run typecheck`、`npm run build`、ruff、mypy 103 个源文件、OpenSpec strict 16/16 与 `git diff --check` 均通过；Python 保留 9 条既有 warning。

**结果与残余风险**：

- 已确认网页可见的唯一 `202608` 文件夹并非重复项，原故障来自客户端响应字段假设错误。
- 线上只读诊断 Pod 未创建、删除或重命名任何 IMA 内容；失败的临时 Pod 保留，删除需独立授权。

### feat(worker)：调整 ima 月份归档格式

**改了什么**：

- 从本地归档文件名提取 `YYYYMM`，移除 ima 文件名的 `YYYYMMDD-` 前缀，并在知识库根目录分页定位唯一的月份文件夹。
- 重名预检与 `add_knowledge` 使用同一个真实 `folder_id`；月份文件夹缺失、重名或不是 `folder_` 标识时 fail closed，不退回根目录。
- ima 专用 Markdown 只保留标题和正文，在正文底部追加来源链接，不再生成“文章信息”区块；本地 Git 权威归档保持原样。

**如何验证**：

- RED：`npm run test --workspace @inbox/worker -- --run tests/ima-article-mirror.test.ts`，8 项中 2 项因旧“文章信息”区块和旧日期前缀文件名失败，其余 6 项通过。
- GREEN：同命令 9/9；新增月份文件夹缺失/不唯一 fail closed 用例，并锁定 `check_repeated_names`、`create_media` 与 `add_knowledge` 请求字段。
- REFACTOR：先构建 `@inbox/domain` 后，Worker 123/123、TypeScript 全工作区 242/242、Python unit/integration 261/261 均通过；workspace typecheck/build、ruff、mypy 103 个源文件和 OpenSpec strict 均通过。

**结果与残余风险**：

- ima OpenAPI 没有创建文件夹接口；`YYYYMM` 文件夹需在每月首次归档前预先创建，否则任务会进入既有重试/DLQ，创建后可安全重放。
- 本次不迁移或重放既有 ima 条目，不执行线上部署或真实文章写入。
- Python 测试保留 9 条既有 warning；首次直接执行工作区测试因尚未生成 `@inbox/domain` 构建产物而失败，按项目既有顺序构建 domain 后复验全绿，不属于本次功能回归。

### fix(worker)：为 ima 转换可检索的文章元数据

**改了什么**：

- 新增纯函数生成 ima 专用 Markdown 副本，只识别文件开头合法 YAML frontmatter；本地 Git 权威归档与传入内容保持不变。
- 把存在的标题、来源、归档时间、作者、发布时间与标签渲染为正文顶部的标题和“文章信息”列表；没有合法 frontmatter 或无法解析时按原文上传。
- `create_media`、COS PUT 与 `add_knowledge.file_info` 统一使用转换后字节数；完成标记继续使用原始内容摘要，幂等边界不变。

**如何验证**：

- RED：`npm run test --workspace @inbox/worker -- --run tests/ima-article-mirror.test.ts`，8 项中新增 2 项因 `renderImaMarkdownCopy` 不存在而失败，既有 6 项通过。
- GREEN：同命令 8/8；`npm run typecheck --workspace @inbox/worker` 通过。
- TypeScript 全量测试 API 66、Console 15、Worker 122、Domain 8、Release 30 全通过；`npm run typecheck` 与 `npm run build` 通过。
- Python `ruff` 通过，unit/integration 261 项通过并保留 8 条既有 warning，mypy 对 103 个源文件无问题。
- OpenSpec strict valid、`git diff --check`、冲突标记检查与秘密扫描通过；GitNexus upstream impact 与最终 detect changes 风险均为 low。

**结果与残余风险**：

- ima OpenAPI 没有文章作者、来源、发布时间或标签的结构化字段，因此这些值以可展示、可检索的 Markdown 正文保存；ima 的系统标题仍使用文件名。
- 本次未新增远程调用、配置、遥测属性或部署动作；线上既有条目不会自动回填，只有后续新镜像或明确重放的条目使用新格式。

### ops(worker)：启用并验收线上 ima Markdown 镜像

**改了什么**：

- 在 Sealos `ns-tbs948af` 创建独立 Opaque Secret，仅保存 `IMA_OPENAPI_APIKEY` 与 `IMA_OPENAPI_CLIENTID`；两个键均非空，验收过程未读取或回显其值。
- Worker StatefulSet 追加该 Secret 的 `envFrom` 引用，并将 `IMA_MIRROR_ENABLED` 从 `false` 切换为 `true`；mihomo、WARP 及其它配置保持不变。
- 使用不含个人数据的合成 Markdown 执行一次真实 ima 镜像，并以相同来源 URL 再执行一次验证幂等。

**如何验证**：

- Kubernetes server-side dry-run 确认补丁仅影响主容器的 Secret 引用与 ima 开关；实际应用后 generation 与 observedGeneration 均为 47，currentRevision 与 updateRevision 一致，readyReplicas 与 updatedReplicas 均为 1。
- Pod 最终三个容器均 Ready；`/healthz` 与 `/readyz` 均返回 200，browser、mihomo、warp 状态均为 `ready` 且 `canAcceptWork=true`。
- 首次合成 Markdown 镜像产生 5 次远程请求，并产生低基数事件 `article.ima_mirror.succeeded`、`stage=complete`、`result=succeeded`。
- 相同来源 URL 第二次镜像产生 0 次远程请求，证明 `/data/ima-mirror` 持久化完成标记生效。

**结果与残余风险**：

- ima 线上镜像与重投幂等验收通过；验证输出不包含凭据、Markdown 正文、文件名、知识库 ID 或第三方响应正文。
- 冷启动期间 mihomo 与 WARP 各因既有 startup probe 时序重启 1 次，随后自行恢复；主 Worker 未重启，最终 StatefulSet rollout 成功。该启动时序不影响本次 ima 验收结论，但后续可单独优化。
- Cloudflare Access 的匿名重定向、Google/邮箱入口与登录后 API Key 解锁仍属于 OpenSpec 5.2，未在本次重复验收。
