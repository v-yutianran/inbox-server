# 2026-09-23 Changelog

### feat(worker)：按配置关闭 Sealos SMTP 通知

**改了什么**：

- Worker 使用 `EMAIL_NOTIFICATIONS_ENABLED` 门控 SMTP 任务，默认 `true`，仅接受 `true` 或 `false`；设为 `false` 时无论 SMTP 凭据是否存在都不连接邮件服务器。
- Sealos Worker 清单显式设为 `false`；其它未配置此开关的部署维持邮件通知默认行为。Telegram 继续独立发送，Telegram 失败时保留现有 `telegram_notification_failed` JSON warn，不发邮件回退。
- 运维手册记录开关默认值、Sealos 设置和回滚到旧镜像可能恢复邮件的风险。未新增依赖或 OpenTelemetry 信号。

**如何验证**：

- RED 配置：`npm run test --workspace @inbox/worker -- tests/config.test.ts`，6 项中 3 项目标断言失败，覆盖默认/false 解析与拒绝 `yes`。
- RED 通知：`npm run test --workspace @inbox/worker -- tests/notifications.test.ts`，4 项中 2 项目标断言失败，分别是关闭后仍调用 SMTP、Telegram 失败后仍尝试 SMTP。
- RED 部署：`uv run pytest tests/unit/test_deployment_files.py --tb=short`，1 failed/11 passed，唯一失败为邮件开关环境值为 None。
- GREEN/REFACTOR：配置 6/6、通知 4/4、部署文件 12/12；组合聚焦命令 `npm run test --workspace @inbox/worker -- tests/config.test.ts tests/notifications.test.ts` 为 10/10；Worker typecheck/build 通过。
- `npm test` 首次因 `npm ci` 后 `@inbox/domain/dist` 尚未构建而使 API/Worker 无法加载模块。运行 `npm run build --workspace @inbox/domain` 后原样重跑 `npm test` 通过：API 66、Console 15、Worker 127、Domain 8、release-operations 30，共 246。`npm run typecheck` 与 `npm run build` 通过。
- `uv run ruff check src/inboxserver tests scripts` 通过；`uv run mypy src/inboxserver --ignore-missing-imports` 通过并给出 2 条 annotation-unchecked notes；`uv run pytest tests/unit tests/integration -m "not e2e" --tb=short` 为 266 passed/9 warnings。
- `openspec validate disable-sealos-email-notifications` 通过。docs audit exit 0，报告 5 条既有 warning。
- git-manager config/inspect/status 成功；当前 `main` 与 `origin/main` 同步，工作树有未暂存和未跟踪改动，本任务未提交。GitNexus `detect_changes` 使用 scope all、repo `inbox-server`、current checkout，结果 medium、2 flows；图谱为 lower-bound，源码核对确认直接入口。

**结果与残余风险**：

- 本次验证是本地调用选择与配置行为证据；没有运行 E2E、真实 Telegram 通知、push 或生产部署。线上 StatefulSet 当前 1/1 Ready、revision `6fd4695b7c`，邮件开关尚未配置，因此线上仍运行旧行为，发布路径待确定；Telegram 送达尚未验收。
- 回滚到不识别新环境变量的旧镜像时，SMTP 凭据仍在可能恢复邮件通知；回滚前须核对目标镜像行为。
- 此变更只门控既有 SMTP 调用，未新增 Span、Metric 或 Log；Worker 源码与依赖未使用 OpenTelemetry SDK / Semantic Conventions，项目未固定规范版本，因此不声明 OpenTelemetry 合规。生产运行时观测仍待部署后确认。
